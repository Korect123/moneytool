# 进度记忆 · 账本 Ledger

> 追加式，只往下加，不删旧的。每次会话结束前更新「当前状态 / 下一步」。
> 新会话开始时，AI 读这个文件就能接上上次的工作。

## 当前状态（最新：2026-07-14）

- **线上 `main` 已包含全部改动（至 `603ca99`），Vercel 已部署，无未推送的提交。**
- 单文件 `index.html` + 两个后端函数：
  - `api/deepseek.mjs`（AI 记账 / 问账本 / 财务总结，需 Supabase 鉴权）
  - `api/rates.mjs`（**新增**：汇率代理，ECB 官方数据，免费无需 key，支持最新 + 历史日期）
- 功能已相当完整：记账（手动/AI/截图OCR/批量/周期账单）、多账户多币种、**汇率折算**、
  预算、存钱目标、月度回顾、账单查询、问账本、图表统计、导入导出、**深浅双主题**。
- 现在能正常跑：✅

## 待办 / 下一步（按痛感排序）

- [ ] **挪钱 / 借款记录不可编辑** —— `recordHTML` 里 `(!isTransfer && !r.loan)` 才给 ✎ 按钮，
      记错只能删了重记（还要重新扣一遍余额）
- [ ] **跨币种换汇记不了** —— 挪钱禁止跨币种；人民币换澳币/港币（出 ¥5000 / 进 A$1000，两边金额不同）
      系统里无解，只能偷偷改余额、不留记录。**去香港后会更痛**
- [ ] 最近记录加「再记一次」一键复制（工作量小、纯复用 `prefillFormFromAI`，性价比最高）
- [ ] 手势滑动删除（全文 0 处 touch 事件）、语音记账、骨架屏（加载时白屏）
- [ ] 自定义分类（`CATS` 写死在代码里，账户能自定义、分类不能）
- [ ] 年度 / 自定义区间统计（图表只有近 6 个月）
- [ ] 支持「借入」（欠别人钱；现在只有 `borrower` 借出）
- [ ] 本位币切换（去香港后启用；**数据结构已就绪**，改动不大）

## 决策记录（为什么这么做，别推翻）

<!-- 每条格式：日期 - 结论 - 涉及文件 - 原因 -->

- 2026-XX-XX - 数据用单个 JSON blob 存 Supabase，不拆多表 - `saveToCloud`/`loadFromCloud` - 简单、够用，暂不改。
- 2026-07-05 - `data` 新增顶层字段 `savingsGoal`，并在 `migrateData()` 追加 backfill - 用户批准：
  新增字段不改名不删除，老数据不受影响。红线已同步到 `CLAUDE.md`。
- 2026-07-05 - 存钱目标采用「绑定真实账户，挪钱即存入，进度=账户余额」，**纯手动**存入 -
  用户收入会变化，且紧急基金必须是真实隔离的钱，不做虚拟计数器。
- 2026-07-05 - 月度回顾定位为**纯展示层**（分步揭晓的仪式感），不做筛选/下钻，明细一键跳账单页 -
  避免与已有的账单筛选、问账本查询重复建设。
- 2026-07-05 - 不加 `navigator.vibrate` 震动反馈 - 用户主用 iPhone，iOS 网页不支持震动。

- **2026-07-13 - 🚨 数据安全三道防线（最重要，绝不可回退）** - `saveToCloud`/`loadFromCloud`/`debouncedSync` -
  修复了一个**会永久销毁全部历史数据的致命 bug**：旧代码在 `loadFromCloud()` 失败时（网络抖动/token 失效），
  `data` 停留在空初始值且照常渲染；此后用户**任何一个写操作**都会触发 `saveToCloud()`，
  **用空对象 upsert 覆盖云端** → 185 笔记录与全部余额永久销毁。现在：
  ① `cloudLoadOk` 守卫：云端未读成功 → `saveToCloud` 直接返回，一次 upsert 都不会发出（已实测断言 upsert 次数 = 0）；
  ② localStorage 本地兜底（key 按 `user.id` 隔离），`debouncedSync` **立即**写本地再防抖同步云端；
     启动时若本地有未同步且更新的数据 → 优先采用并推送；
  ③ 同步失败：醒目 warn toast + 退避重试（5s/15s/60s）+ 监听 `online` 事件立即重试。

- **2026-07-13 - 汇率体系：每笔冻结当日汇率** - `index.html`（汇率区块）+ `api/rates.mjs` -
  修复了**多币种被直接相加**的 bug（¥100 + A$100 曾被算成 ¥200，正确应为约 ¥570）。原则：
  ① 每条 record 新增 `fx` 字段（1 单位该币种 = 多少人民币，**记账当日冻结**），
     `migrateData` 给 CNY 记录补 1；**历史账单不随汇率波动而改变**（已实测：篡改当前汇率后旧记录折算值不变）；
  ② 所有跨账户聚合（本月收支/预算/图表/AI 总结/账单汇总/月度回顾）**一律经 `sumCny()` 折算后再算**；
     单账户视角（账户明细的本月支出、存钱目标存入额）**保持原币不折算**；
  ③ 汇率不可用时 **`cnyOf()` 返回 null**、外币只显示原币、聚合中排除并标注「N 笔外币未折算」——**绝不瞎猜**；
  ④ 设置页「补全历史汇率」按记录**实际日期**拉当天真实汇率回填（幂等，按日期去重）；
  ⑤ **扣款逻辑 `deductExpense` 零改动**（余额永远按账户原币扣）；本位币暂固定 CNY。

- **2026-07-14 - 配色分层（重要：两套色板不要合并）** - `CAT_META` / `CAT_CHART_LIGHT` / `CAT_CHART_DARK` -
  ① `CAT_META` / `CAT_COLORS`（淡雅中国传统色）→ **仅供小尺寸元素**：九宫格图标、记录行图标、
     进度条、图例小圆点。在 16px 尺度下低饱和 = 雅致。
  ② `CAT_CHART_LIGHT` / `CAT_CHART_DARK` → **仅供图表大色块填充**，由 `catChartColor()` 按 `data-theme` 切换。
     **为什么必须两套**：白底（宣纸）需要"够深够艳"才有对比，墨底需要"提亮"才不会沉下去 ——
     方向相反，**一套色不可能同时满足**。两套均经 WCAG 对比度校验（对各自背景 ≥3.0）。
  ③ 柱状图的 **1.5px 描边**（`--pie-border`）是"不糊"的关键——同一套色，有缝的饼图清爽、
     没缝的柱图糊成一片。**别删描边。**

- **2026-07-14 - 印章 vs 撒花：不同场景，别混用** - `showToast()` / `celebrate()` -
  **印章**（朱红方印，旋转落定 + 墨渗，0.42s）= **日常每笔**记账成功的确认，在 toast 内；
  **撒花**（`celebrate()`）= **里程碑达成**（存钱目标）才放。
  ⚠️ **已否决**：在新记录行上盖章 —— 实测记录行**没有安全留白**，印章必然压住金额或账户名，
  牺牲可读性换装饰不值得，已移除。

- **2026-07-14 - View Transitions 渐进增强** - `withTransition()` -
  点记录 → 平滑变形为编辑弹窗；点钱包卡片 → 展开为账户明细。
  浏览器不支持 / `prefers-reduced-motion` → **自动退回原 `modalIn` 效果**，功能零影响。
  已处理快速连点导致的 `InvalidStateError`（吞掉打断异常，功能由 `fn()` 保证完成）。

## 已知不能碰的地方

- `migrateData()`：**已有**迁移逻辑不许改动或删除；为新字段追加 backfill 是允许且推荐的（2026-07-05 更新）。
- `data` 顶层字段：现有六个 key 不许改名或删除；新增允许但必须在 `migrateData` 补默认值（2026-07-05 更新）。
- **`cloudLoadOk` 守卫**：这是防数据销毁的最后一道闸，**任何情况下不许绕过或删除**（2026-07-13）。
- **`deductExpense`**：余额永远按账户**原币**扣，不参与汇率折算（2026-07-13）。

## 协作方式（备忘）

- **本机没有 GitHub CLI 凭据**，AI 推不了代码。推送一律由用户在 **GitHub Desktop** 完成：
  选中分支 → 按钮若显示 **`Push origin/main`** 则直推即生效（无需 PR）；
  若显示 **`Publish branch`** 则还需在 GitHub 网页开 PR 并 Merge 才会部署。
- **自动化测试纪律**：必须用**全新端口 + 无登录态**的本地源（`python3 -m http.server <新端口>`），
  注入测试用户前先断言登录墙存在、`localStorage` 为空。
  ⚠️ 教训（2026-07-02）：曾在残留真实登录 session 的端口上跑自动化，误入真实账本
  （云端经比对无损，测试身份被 Supabase RLS 拦截）。
- 后台标签页的 `requestAnimationFrame` 与 `setTimeout` 会被 Chrome 限流 ——
  自动化里数字滚动/防抖同步"看起来没跑"通常是这个原因，不是 bug。

## 工作日志

### 2026-07-05（动效增补，分支 feat/motion-phase1-number-rolling → 已上线）

- Phase 1：`animateNumber`/`setAnimatedNum` 数字滚动（总资产分币种、净收支、月收支笔数、钱包卡片、
  记一笔页统计、账户明细余额）；运行时判断 reduced-motion。
- Phase 2：主按钮弹性回弹、成功 toast 的对勾、新记录行入场动画（7 个记账入口全覆盖）。
- Phase 3：月度回顾弹窗（分步揭晓 + 数字从 0 滚动）、存钱目标（进度卡 + 「存入」预选挪钱）、
  里程碑手写 canvas 撒花。

### 2026-07-11 ~ 07-12（AI 记账可用性）

- **AI 单笔识别结果改为填入可编辑表单**：原本是只读预览（只有账户能改），AI 猜错（如把退款当支出）
  只能"确认记错的"或重来。现在识别后直接预填「记一笔」表单，类型/金额/分类/备注/账户/日期全部可改，
  保存仍走现有 `addRecord`（**扣款逻辑零改动**）。多笔识别维持原只读预览。
- **退款识别**：`api/deepseek.mjs` 提示词新增规则 —— 出现「退款/退货/返现/报销…」等词时 `type=income`，
  并新增「退款」收入分类（`CATS.income` + 金色 meta + 返还箭头图标）。

### 2026-07-13（数据安全 + 汇率，两个 P0）

- **数据安全**：见上方决策记录（`cloudLoadOk` 守卫 + 本地兜底 + 退避重试）。
  验证：**故意模拟云端读失败**后做写操作 → 断言 `upsert` 次数 = 0（旧代码在此场景会清空云端）；
  记 3 笔后立刻查 localStorage 已在；模拟断网 → 存本地 + 警告 + 排重试；恢复网络 → 自动同步成功。
- **汇率体系**：见上方决策记录。验证：¥100 + A$100 从错误的 ¥200 变为正确的 ¥570.57；
  篡改当前汇率后历史记录折算值不变（冻结生效）；补全历史汇率实测 4/26 拉到真实汇率 4.8842。

### 2026-07-13 ~ 07-14（记一笔表单体验）

- 备注**常驻**（原本必须先选分类才出现）；账户从原生 `<select>` 换成**一排胶囊按钮**
  （隐藏的 `<select id="wallet">` 保留为数据源，六处读写逻辑零改动）；移动端输入框字号抬到 16px
  修复 iOS 聚焦自动放大。
- 「再记一笔」批量行：**备注/日期常驻**（去掉折叠按钮）；账户胶囊补上**彩色图标 + 账户名**，独占一行，
  账户增多时自动换行（12 个账户实测无溢出）。
- 「记一笔」右栏新增**可滚动的最近记录面板** + 「查看全部账单 →」入口，sticky 跟随；
  纯复用 `recordHTML()`（编辑/删除按钮白送）+ 挂在 `renderAddPage()`（自动保持最新）。

### 2026-07-14（视觉与交互）

- **视觉升级**：分类色板从高饱和彩虹色换成中国传统色；新增**深色模式（墨色夜纸）**
  （清理 22 处硬编码 `#fff`，图表颜色改为读 CSS 变量并在切主题时重绘）；
  登录页的水墨晕染**复用到全站**（透明度 .4、周期 72-88s，移动端与 reduced-motion 下降为静态省电）。
- **设置页重构**：从写死 `max-width:520px` 的单列窄条 → **自适应卡片网格**（宽屏 3 列、手机 1 列），
  10 张带金色线性图标的卡片；修复余额输入框因账户名长短不一导致的参差对齐。
  所有 id 与 onclick 原样保留，渲染函数零改动。
- **新颖交互**：朱红**印章**确认 + **View Transitions** 共享元素过渡（见决策记录）。
- **图表配色**：改为**亮/暗两套独立色板**（见决策记录）。

### 遗留 / 观察

- 月度回顾里「当月存入为 ¥0.00」时仍显示该行，可考虑隐藏。
- 图表默认只有近 6 个月，无年度视图。
- Chart.js 的入场动画时长/缓动尚未微调（原动效 Phase 4，一直未做，非必要）。
