# 进度记忆 · 账本 Ledger

> 追加式，只往下加，不删旧的。每次会话结束前更新「当前状态 / 下一步」。
> 新会话开始时，AI 读这个文件就能接上上次的工作。

## 当前状态（最新）

- 版本 / 大概进度：单文件 `index.html` 版本，功能完整（记账、多账户、图表、周期账单、AI 问账本）。动效增补 Phase 1-3 已完成并验证，在分支 `feat/motion-phase1-number-rolling` 上。
- 现在能正常跑：✅
- 下一步想做：动效 Phase 4（Chart.js 入场/切换微调，可选）；分支推送到 GitHub（本机缺推送凭据，待用户处理）。

## 待办

- [ ] 推送 `feat/motion-phase1-number-rolling` 分支并合并（需要用户提供 GitHub 凭据或自行推送）
- [ ] Phase 4：Chart.js 动画时长/缓动微调（可选，未确认要不要做）

## 决策记录（为什么这么做，别推翻）

<!-- 每条格式：日期 - 结论 - 涉及文件 - 原因 -->

- 2026-XX-XX - 数据用单个 JSON blob 存 Supabase，不拆多表 - `saveToCloud`/`loadFromCloud` - 简单、够用，暂不改。
- 2026-07-05 - `data` 新增顶层字段 `savingsGoal`（存钱目标：name/accountId/target/celebrated），并在 `migrateData()` 追加一行 backfill（`savingsGoal` 缺省补 `null`）- `index.html`（migrateData / 存钱目标区块）- 用户批准：新增字段不改名不删除，老数据不受影响；数据结构安全演进的标准做法就是"新增 + migrateData 补默认值"。红线已同步更新到 `CLAUDE.md`。
- 2026-07-05 - 存钱目标采用"绑定真实账户，挪钱即存入，进度=账户余额"，**纯手动**存入，不做自动比例提示 - `index.html`（存钱目标区块）- 用户收入会变化，且紧急基金必须是真实隔离的钱，不做虚拟计数器。
- 2026-07-05 - 月度回顾定位为纯展示层（分步揭晓的仪式感），不做筛选/下钻，明细一键跳账单页 - `index.html`（月度回顾区块）- 避免与已有的账单筛选、问账本日期区间查询重复建设。
- 2026-07-05 - 不加 `navigator.vibrate` 震动反馈 - （未实现）- 用户主用 iPhone，iOS Safari/PWA 不支持网页震动，做了也无感。

## 已知不能碰的地方

- `migrateData()`：**已有**迁移逻辑不许改动或删除；为新字段追加 backfill 是允许且推荐的（2026-07-05 更新）。
- `data` 顶层字段：现有六个 key 不许改名或删除；新增允许但必须在 `migrateData` 补默认值（2026-07-05 更新）。

## 工作日志

### 2026-XX-XX

- 做了：___
- 验证了：___
- 遗留问题：___

### 2026-07-05（含前几日同一分支工作）

- 做了：动效增补三个阶段，全部在分支 `feat/motion-phase1-number-rolling`：
  - Phase 1（已提交 501550c）：`animateNumber`/`setAnimatedNum` 数字滚动，接入总资产（分币种）、净收支、月收入/支出/笔数、钱包卡片、记一笔页统计、账户明细余额；运行时判断 reduced-motion。
  - Phase 2（已提交 a9c9081）：主按钮弹性回弹、toast 成功文案 "✓" 自动替换为 SVG 描边对勾、新记录行一次性入场动画（7 个记账入口全覆盖）。
  - Phase 3（本次提交）：月度账单回顾弹窗（统计页「✦ 月度回顾」按钮，分步揭晓+数字从 0 滚动）、存钱目标（总览进度卡+设置页管理+「存入」预选挪钱）、里程碑手写 canvas 金色撒花。
- 验证了：隔离本地服务器 + Chrome 真实 UI 驱动：记账/补记/挪钱/删除撤销后数字滚动与格式逐字符一致；快速连记打断动画正确；reduced-motion 全部瞬时；里程碑跨越触发撒花与 toast；回顾分步揭晓、月份切换、跳转账单页正确；控制台零报错。
- 遗留问题：分支未推送（本机无 GitHub 凭据）；「回顾里当月存入为 ¥0.00 时仍显示该行」可考虑隐藏；Phase 4 待定。
- ⚠️ 教训（2026-07-02）：本地预览源 127.0.0.1:8765 上存有用户真实登录 session，自动化测试一度误入真实账本（云端经比对无损，测试身份被 RLS 拦截）。此后自动化测试必须用全新端口/无 session 的源，注入假用户前先检查登录墙。
